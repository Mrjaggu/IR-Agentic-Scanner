## What & why

<!-- What does this change, and why -- what problem does it solve or what
     does it enable? Link an issue if one exists. -->

## How it was verified

<!-- What did you actually run/click to confirm this works? See
     CONTRIBUTING.md's "Before opening a PR" section. A PR without this
     section is hard to review with confidence. -->

- [ ] `python3 -m py_compile` on changed Python files (if applicable)
- [ ] `node --check` on the extracted frontend `<script>` block (if `frontend/ir_platform_app.html` changed)
- [ ] Exercised the actual code path (route/CLI/UI click-through), not just a syntax check
- [ ] Updated `README.md` / `ROADMAP.md` if this adds a feature, a new `.env` variable, or changes setup

## Anything the reviewer should know

<!-- Trade-offs, things you considered and rejected, known limitations,
     follow-up work you're deliberately leaving out of this PR. -->
